#pragma once

#include <cstdint>

namespace charutils
{
    bool isLetter(uint8_t character);
    bool isUpperCase(uint8_t character);
    bool isLowerCase(uint8_t character);
} // namespace charutils
