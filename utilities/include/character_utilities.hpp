#pragma once

#include <cstdint>

namespace charutils
{
    bool isLetter(uint8_t character);
    bool isUpperCase(uint8_t character);
    bool isDownCase(uint8_t character);
} // namespace char_utils
